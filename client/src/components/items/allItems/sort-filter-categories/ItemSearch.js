import { Container, Form, FormGroup, Input, Label } from 'reactstrap';

export const ItemSearch = ({ setAllItems }) => {
  return (
    <>
      <div>
        <Form>
          <FormGroup>
            <Input />
          </FormGroup>
        </Form>
      </div>
    </>
  );
};
